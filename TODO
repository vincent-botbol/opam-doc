(** comm record field*) -> fix the parser bug when there is no space

It would be nice to have an ifram on types from other modules when we hover on them (with an ajax request)

Try to let people customize the stylesheets. For each element, have some syntax to describe the output format?
